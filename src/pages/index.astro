---
import BaseLayout from '../layouts/BaseLayout.astro';
import HorizontalCard from '../components/HorizontalCard.astro';
import CardListLayout from '../layouts/CardListLayout.astro';
// import { getCollection } from 'astro:content';
import { projects } from './projects.astro';

// const posts = (await getCollection('blog')).sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());

const keywords = ['Angular', 'Node.js + Express', 'SQL Server'];

// const last_posts = posts.slice(0, 3);

const last_projects = projects.slice(0, 3);
---

<BaseLayout sideBarActiveItemID="home">
	<div class="pb-12 mt-5">
		<div class="text-xl py-1">Hey there 👋</div>
		<div class="text-5xl font-bold">I'm Ron Strauss</div>
		<div class="text-3xl py-3 font-bold">Fullstack Developer</div>
		<div class="py-2">
			<text class="text-lg">
				<p>
					I'm an experienced and passionate web developer, currently employed at <b>Mobility Software Ltd.</b> full time, but I still enjoy contributing to <span
						class="italic text-primary font-medium">
						Open Source
					</span> from time to time.
				</p>
				<p>My day-to-day work mainly involves {keywords.map((word) => <b>{word}, </b>)} and everything inbetween.</p>
			</text>
		</div>
		<div class="mt-8">
			<!-- <a class="btn" href="https://twitter.com/manuelernestog" target="_blank"> Let's connect!</a>
			<a href="https://github.com/manuelernestog/astrofy" target="_blank" class="btn btn-outline ml-5"> Get This template</a> -->
		</div>
	</div>

	<div>
		<div class="text-3xl w-full font-bold mb-2">My last projects {'</>'}</div>
	</div>

	<CardListLayout list={last_projects} />

	<!-- 
  

  <div>
    <div class="text-3xl w-full font-bold mb-5 mt-10">Latest from blog</div>
  </div>

  {
    last_posts.map((post) => (
      <>
        <HorizontalCard
          title={post.data.title}
          img={post.data.heroImage}
          desc={post.data.description}
          url={"/blog/" + post.slug}
          target="_self"
          badge={post.data.badge}
        />
        <div class="divider my-0" />
      </>
    ))
  } -->
</BaseLayout>
