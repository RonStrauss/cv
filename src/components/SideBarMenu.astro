---
const { sideBarActiveItemID } = Astro.props;
import { Image } from 'astro:assets';
const activeClass = 'bg-base-300'; // For primary color replace with `active` class
const routes = [
	{ id: 'home', path: '/', text: 'Home' },
	{ id: 'projects', path: '/projects', text: 'Projects' },
	// { id: 'services', path: '/services', text: 'Services' },
	// { id: 'store', path: '/store', text: 'Store' },
	// { id: 'blog', path: '/blog/', text: 'Blog' },
	{ id: 'cv', path: '/cv', text: 'CV' },
];
import emailSVG from '../images/email.svg';
---

<ul class="menu grow shrink menu-md overflow-y-auto">
	{
		routes.map((route) => (
			<li>
				<a class="py-3 text-base" id={route.id} href={route.path}>
					{route.text}
				</a>
			</li>
		))
	}
	<li>
		<a class="py-3 text-base flex justify-between" href="mailto:ron.strauss.work@gmail.com" target="_blank" referrerpolicy="no-referrer-when-downgrade"
			>Contact

			<Image height="30" alt="email" src={emailSVG} format="svg" />
		</a>
	</li>
</ul>

<script define:vars={{ sideBarActiveItemID: sideBarActiveItemID, activeClass: activeClass }}>
	const activeItemElem = document.getElementById(sideBarActiveItemID);
	activeItemElem && activeItemElem.classList.add(activeClass);
</script>
